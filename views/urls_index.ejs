<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp - Home</title>
</head>

<body>
  <header>
    <% include partials/_header %>
  </header>
  <div class="container">
    <% if (users) { %>
      <table class="table">
        <thead>
          <tr>
            <th>Short Link</th>
            <th style="width: 300px">Full Link</th>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody>
          <% for (const links in urls) { %>
            <tr>
              <td>
                <a href="/u/<%= links %>">
                  <%- links %>
                </a>
              </td>
              <td>
                <%= urls[links].longURL %>
              </td>
              <td>
                <a href="/urls/<%= links %>">Edit</a>
              </td>
              <td>
                <form method="POST" action="/urls/<%= links %>/delete">
                  <input type="submit" value="Delete" />
                </form>
              </td>
            </tr>
            <% } %>
              <tr>
                <td colSpan="2">
                  <a href="/urls/new">Shorten a New Link</a>
                </td>
              </tr>
        </tbody>
      </table>
      <% } else {%>
        <p>Please Login or Register</p>
        <% } %>
  </div>
</body>

</html>